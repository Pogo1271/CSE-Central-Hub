import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "EPOS Central Hub",
  description: "A centralized platform for managing companies, products, inventory, quotes, calendar events, and user permissions.",
  keywords: ["EPOS", "Point of Sale", "Business Management", "Inventory", "Quotes"],
  authors: [{ name: "EPOS Central Hub Team" }],
  openGraph: {
    title: "EPOS Central Hub",
    description: "Centralized platform for business operations management",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
